# Homework 13 (Mock)

Run:

```bash
python homework13_mock_api.py
```

Optional env var:
- `MOCK_API_KEY` (default: `demo-key`)

The script starts a local mock AV/FW API, authenticates with API key, performs a JSON request, and prints scan status and firewall rules.
